    <div class="row content">
        <div class="col-md-6 col-md-offset-3">
<%= form_with(model: @task, url: freelancer_tasks_path, local: true) do |form| %>

<% if @task.present? && @task.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@task.errors.count, "error") %> prohibited
        this info from being saved:
      </h2>
      <ul>
        <% @task.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group ">
    <%= form.label :work_description %>
    <%= form.text_area :work_description , class: 'form-control' %>
     </div>
  <div class="form-group ">
    <%= form.label :date %>
    <%= form.date_field :date , class: 'form-control' %>
   </div>

  <div class="form-group ">
    <%= form.label :income %>
    <%= form.text_field :income , class: 'form-control'  %>
   </div>

   <div class="form-group ">Customer
  <% form.label :customer  %>
  <%= form.text_area :customer , class: 'form-control' %>
    </div>
  <p>
    <%= form.submit "Add", class: "btn btn-primary" %>
  </p>
<% end %>
 </div>
</div>

